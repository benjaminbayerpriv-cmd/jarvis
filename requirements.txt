fastapi==0.115.6
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
requests==2.32.3

# Der Code-Tab nutzt unter Windows einen ConPTY-Pseudo-Terminal (pywinpty);
# unter macOS/Linux kommt das stdlib-PTY ohne Zusatzabhängigkeit aus.
pywinpty>=2.0.11; platform_system == "Windows"

python-multipart==0.0.32

# Global hotkey listener (launcher/hotkey_listener.py).
pynput==1.7.7

# Primary TTS voice (backend/tts.py) — local, offline, unlimited.
supertonic==1.3.1

# Local STT (backend/stt.py) — replaces the browser's Web Speech API.
faster-whisper==1.2.1

# delete_path (backend/tools.py) — moves files/folders to the Trash instead
# of a raw `rm -rf`, so a deletion is reversible and never silently no-ops.
send2trash==1.8.3

# Spells out large numbers for the local voices (backend/tts.py) — Supertonic
# and the OS voices read raw digit strings one character at a time instead
# of as a number otherwise.
num2words==0.5.14

# Only used by backend/live_voice_test.py (Gemini Live speech-to-speech
# proof-of-concept, not part of the normal Jarvis pipeline).
sounddevice==0.5.6
numpy==2.5.2
websockets==17.0.1

# Discord-Bot (backend/discord_bot.py) — privater Voice-Channel als echtes
# Sprachgespräch (Whisper-Transkription + LLM-Antwort + TTS zurück) + DM-
# Pings. Optional: ohne DISCORD_BOT_TOKEN in .env bleibt die ganze
# Integration ein No-Op. discord-ext-voice-recv ist noch alpha (0.5.x) — das
# ist die einzige verfügbare Bibliothek, die discord.py um Audio-Empfang
# erweitert (discord.py selbst kann nur senden). discord.py[voice] zieht
# `davey` (DAVE-E2EE-Voice) mit — Pflicht, da manche Server (ältere/größere)
# DAVE inzwischen server-seitig erzwingen; ohne davey kann der Bot dort gar
# nicht erst verbinden (RuntimeError). Auf neueren/kleineren Servern ohne
# erzwungenes DAVE klappt der Verbindungsaufbau auch ohne davey.
discord.py[voice]==2.7.1
PyNaCl==1.5.0
discord-ext-voice-recv==0.5.2a179
