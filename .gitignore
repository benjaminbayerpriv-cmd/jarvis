.env
__pycache__/
*.pyc
.venv/
venv/
jarvis_notes.md
Jarvis/
launcher/*.log
backend/transcript.log
backend/conversations/
backend/projects/
backend/vector_index/
.DS_Store

# Ad-hoc local safety copies (e.g. before a big UI redesign) — not source,
# git history already has the old versions.
/backups/

# PyInstaller build output for the .app/.exe launcher — generated by
# launcher/build_app.sh / build_exe.bat, not source.
launcher/build/
launcher/dist/
launcher/*.spec
